{
  "name": "wom-tracker-web",
  "private": true,
  "description": "Tests for the browser half of WOM Tracker. The app itself is Python; this exists only so the 2300 lines of JavaScript it serves are not the one part nothing checks.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "cd tests/js && node --test"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
